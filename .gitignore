# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Virtual environments
.venv/
venv/
env/

# App data / caches (the HTTP cache is regenerable; never commit it)
*.sqlite
*.sqlite3
*.db
data/
cache/
instance/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Env files
.env
.env.*
!.env.example

# Generated artifacts
out/

# ...but DO commit the seed catalogue: a catalogue enriched offline (Gilad is not
# reachable from CI) that the daily build restores to recover skill levels.
# Gzipped, so the *.sqlite rule above doesn't catch it; pinned here for safety.
!seed/
!seed/catalog.sqlite.gz
